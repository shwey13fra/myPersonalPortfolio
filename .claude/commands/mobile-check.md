Check my portfolio website for mobile responsiveness. Test it against these criteria:

- All sections stack properly on screens below 768px width
- Text is readable without zooming on a phone
- Images scale correctly and don't overflow their containers
- Buttons and links are at least 44px tall (easy to tap with a finger)
- No horizontal scrolling on any screen size
- The email gate (if it exists) works well on mobile
- Nothing gets cut off, hidden, or overlapping

Fix every issue you find. Tell me what you fixed.
